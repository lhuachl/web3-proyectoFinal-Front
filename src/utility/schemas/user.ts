/*import UserRol from '../types/userRol';
import UUID from '../types/uuid';
export class User {
    id: UUID;
    name: string;
    email: string;
    rol: UserRol;
    constructor(id: string, name: string, email: string, rol: UserRol) {
        this.id = id;
        this.name = name;
        this.email = email;
        this.rol = rol;
    }}*/